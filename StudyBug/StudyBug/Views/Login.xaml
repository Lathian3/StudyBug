<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:StudyBug.ViewModels"
             x:Class="StudyBug.Views.Login"
             Shell.FlyoutBehavior="Disabled"
             Shell.NavBarIsVisible="False"
             >

    <ContentPage.BindingContext>
        <vm:LoginView/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Login" Command="{Binding LoginCommand}" VerticalOptions="CenterAndExpand" />
            <Button Text="Delete Account" Command="{Binding DeleteAccount}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>